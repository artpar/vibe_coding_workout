To start the project (mac commands)

you may need to install nympy binaries also

```
python3.11 -m venv myenv
source myenv/bin/activate
pip install -r requirements.txt
streamlit run workout_dashboard.py
```

